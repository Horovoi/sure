<%= render DS::Dialog.new do |dialog| %>
  <% dialog.with_header(title: "New transaction") %>
  <% dialog.with_body do %>
    <%= render "form", entry: @entry, income_category_groups: @income_category_groups, expense_category_groups: @expense_category_groups %>
  <% end %>
<% end %>
